<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EasyPosTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/fiservcolor</item>
        <item name="colorPrimaryVariant">@color/azulmarino</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#4CAF50</item>
        <item name="colorSecondaryVariant">#03A9F4</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/colorCashDro</item>
    </style>
    <style name="Theme.EasyPosTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.EasyPosTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.EasyPosTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="MenuItemStyle" parent="Widget.AppCompat.Light.PopupMenu">
        <item name="android:textColor">@color/white</item> <!-- Color blanco (FFFFFF) para el texto -->
    </style>
</resources>